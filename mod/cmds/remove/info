	 --------------- DELETE USER IN LDAP (access) ---------------

	Enter userid (login name) to be use: koko.suryo
	use wildcard (first name)? default to not use wildcard (y/n):
	The user koko.suryo has been removed from LDAP
	Next steps remove the user's VPN certificate in Pfsense
	Pfsense: Cert. Manager -> Certificates -> delete user certificate
	Enjoy a cuppa of hot coffee ☕️  / 🥃
	The end

